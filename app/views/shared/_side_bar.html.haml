.side_bar
  .header
    -# %h3.header__name
    -#   = current_user.name
    .name
      = current_user.name
    %ul.header__lists
      %li.list
      = link_to "#", class: "header__btn" do
        = icon('fa', 'edit')
      %li.list
        = link_to edit_user_path(current_user),class: "header__btn" do
          = icon('fa', 'cog')
      

  .groupbox
    .group
      .groupbox__name1
        タロウ
      .groupbox__message1
        nice!
      .groupbox__name2
        TAROU
      .groupbox__message2
        good!!
      